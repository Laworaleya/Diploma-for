# Backend API URL — set in Render dashboard as environment variable
# In production: https://finlit-backend.onrender.com
# In dev: leave empty (Vite proxy handles /api → localhost:8000)
VITE_API_BASE_URL=

# Telegram bot username (without @)
VITE_TELEGRAM_BOT_USERNAME=FinLit_bot
